# Mirrors config.yaml's brain.host/brain.port -- the Renderer can't read
# that YAML file directly (browser JS), so this is kept in sync by hand.
# Vite exposes VITE_-prefixed vars via import.meta.env.
VITE_BRAIN_WS_URL=ws://localhost:8765
